/**
 * Minimal background — removed floating dots, planes, and neon SVG paths.
 * Kept as a component shell for future subtle additions.
 */
const FlightPathsBackground = () => null;

export default FlightPathsBackground;
